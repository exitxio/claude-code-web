# ── 필수 ───────────────────────────────────────────────────────────────────
# NextAuth 시크릿 (openssl rand -base64 32)
NEXTAUTH_SECRET=your-secret-here
NEXTAUTH_URL=http://localhost:3000

# ── 계정 (최소 하나 이상 설정) ────────────────────────────────────────────
# env 기반 계정 (username:password 쌍, 쉼표로 구분)
USERS=admin:changeme

# ── 서버 설정 ────────────────────────────────────────────────────────────
# Claude 모델 (기본값: claude-sonnet-4-6)
# CLAUDE_MODEL=claude-sonnet-4-6

# automation-server 워커 풀 크기 (기본값: 1)
# AUTOMATION_POOL_SIZE=2
